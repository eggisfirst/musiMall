<view class="swiper-tab" >
  <view class="tab-item {{currentTab==index ? 'on' : ''}}" 
    wx:for='{{list}}' 
    wx:key='{{index}}'
    data-index="{{index}}" 
    catchtap="swichNav">
    {{item}}
  </view>
</view>


 
