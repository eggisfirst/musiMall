<view class="swiper-tab" >
  <view class="tab-item {{currentTab==index ? 'on' : ''}}" 
    wx:for='{{list}}' 
    wx:key='*this'
    data-index="{{index}}" 
    catchtap="swichNav">
    {{item.name}}
  </view>
</view>


 
